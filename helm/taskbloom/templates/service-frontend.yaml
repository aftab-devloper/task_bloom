apiVersion: v1
kind: Service
metadata:
  name: taskbloom-frontend
spec:
  type: NodePort
  selector:
    app: taskbloom-frontend
  ports:
    - port: {{ .Values.frontend.port }}
      targetPort: {{ .Values.frontend.port }}
      nodePort: {{ .Values.frontend.nodePort }}